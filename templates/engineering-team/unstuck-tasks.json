[
  {
    "id": "unstuck-prs",
    "description": "Monitor open PRs for failing CI checks. When a workflow run fails on a PR, comment @copilot to request a fix. This task is handled automatically by the webhook handler when workflow_run events arrive.",
    "status": "pending",
    "role": "evaluator",
    "kind": "recurring",
    "cooldown_seconds": 3600,
    "precondition_prompt": "This task is event-driven: the webhook handler automatically comments on PRs when a workflow_run event arrives with a failure conclusion. Return true to keep the task active."
  }
]
